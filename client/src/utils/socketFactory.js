// import SocketClient from './socketClient_old';

// const socketFactory = {
//   sockets: [],
//   createSocket: ({ name, path }) => {

//     const socket = new SocketClient({ socketPath: path });
//     socketFactory.sockets.push({ name, socket });

//   },
//   getSockets: () => socketFactory.sockets,
//   getSocketByName: name => (socketFactory.sockets.find(n => n.name === name).socket),
// };

// export default socketFactory;
